---
// InputPreview.astro - Static preview of Input variants
---

<div class="input-preview not-content">
  <div class="preview-section">
    <div class="preview-label">Variants</div>
    <div class="preview-column">
      <input type="text" class="input input-default" placeholder="Default input..." />
      <input type="text" class="input input-filled" placeholder="Filled input..." />
    </div>
  </div>

  <div class="preview-section">
    <div class="preview-label">Sizes</div>
    <div class="preview-column">
      <input type="text" class="input input-default input-sm" placeholder="Small input" />
      <input type="text" class="input input-default input-md" placeholder="Medium input" />
      <input type="text" class="input input-default input-lg" placeholder="Large input" />
    </div>
  </div>

  <div class="preview-section">
    <div class="preview-label">States</div>
    <div class="preview-column">
      <div class="input-group">
        <label class="input-label">Normal</label>
        <input type="text" class="input input-default" placeholder="Type here..." />
      </div>
      <div class="input-group">
        <label class="input-label">With Value</label>
        <input type="text" class="input input-default" value="Hello World" />
      </div>
      <div class="input-group">
        <label class="input-label">Error</label>
        <input type="text" class="input input-error" value="Invalid value" />
        <span class="input-error-text">This field is required</span>
      </div>
      <div class="input-group">
        <label class="input-label">Disabled</label>
        <input type="text" class="input input-default input-disabled" placeholder="Disabled" disabled />
      </div>
    </div>
  </div>
</div>

<style>
  .input-preview {
    background: #020617;
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 8px;
    padding: 1.5rem;
    margin: 1.5rem 0;
  }

  .preview-section {
    margin-bottom: 1.5rem;
  }

  .preview-section:last-child {
    margin-bottom: 0;
  }

  .preview-label {
    font-size: 0.7rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: #64748b;
    margin-bottom: 0.75rem;
    font-family: 'JetBrains Mono', monospace;
  }

  .preview-column {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    max-width: 320px;
  }

  .input-group {
    display: flex;
    flex-direction: column;
    gap: 0.375rem;
  }

  .input-label {
    font-size: 0.75rem;
    font-weight: 500;
    color: #94a3b8;
    font-family: 'Inter', system-ui, sans-serif;
  }

  .input-error-text {
    font-size: 0.7rem;
    color: #ef4444;
    font-family: 'Inter', system-ui, sans-serif;
  }

  /* Base Input Styles */
  .input {
    width: 100%;
    font-family: 'Inter', system-ui, sans-serif;
    border-radius: 0;
    transition: all 0.2s ease;
    outline: none;
  }

  /* Sizes */
  .input-sm {
    height: 36px;
    padding: 0 12px;
    font-size: 0.8rem;
  }

  .input-md {
    height: 40px;
    padding: 0 14px;
    font-size: 0.875rem;
  }

  .input-lg {
    height: 48px;
    padding: 0 16px;
    font-size: 1rem;
  }

  .input:not(.input-sm):not(.input-lg) {
    height: 40px;
    padding: 0 14px;
    font-size: 0.875rem;
  }

  /* Variants */
  .input-default {
    background: transparent;
    border: 1px solid rgba(255, 255, 255, 0.2);
    color: #ffffff;
  }

  .input-default::placeholder {
    color: #64748b;
  }

  .input-default:focus {
    border-color: #fbbf24;
    box-shadow: 0 0 0 1px rgba(251, 191, 36, 0.3);
  }

  .input-filled {
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid transparent;
    color: #ffffff;
  }

  .input-filled::placeholder {
    color: #64748b;
  }

  .input-filled:focus {
    background: rgba(255, 255, 255, 0.08);
    border-color: rgba(251, 191, 36, 0.5);
  }

  /* Error */
  .input-error {
    background: transparent;
    border: 1px solid #ef4444;
    color: #ffffff;
  }

  .input-error:focus {
    box-shadow: 0 0 0 1px rgba(239, 68, 68, 0.3);
  }

  /* Disabled */
  .input-disabled {
    opacity: 0.5;
    cursor: not-allowed;
  }
</style>
