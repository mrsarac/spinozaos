---
// ModalPreview.astro - Static preview of Modal component
---

<div class="modal-preview not-content">
  <div class="preview-section">
    <div class="preview-label">Modal Example</div>
    <div class="modal-container">
      <div class="modal">
        <div class="modal-header">
          <h3 class="modal-title">Modal Title</h3>
          <button class="modal-close">&times;</button>
        </div>
        <div class="modal-body">
          <p>This is the modal body content. You can place any content here including forms, text, or other components.</p>
        </div>
        <div class="modal-footer">
          <button class="btn btn-ghost">Cancel</button>
          <button class="btn btn-primary">Confirm</button>
        </div>
      </div>
    </div>
  </div>

  <div class="preview-section">
    <div class="preview-label">Sizes</div>
    <div class="preview-row">
      <div class="size-badge">sm (400px)</div>
      <div class="size-badge">md (500px)</div>
      <div class="size-badge">lg (600px)</div>
      <div class="size-badge">full (100%)</div>
    </div>
  </div>
</div>

<style>
  .modal-preview {
    background: #020617;
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 8px;
    padding: 1.5rem;
    margin: 1.5rem 0;
  }

  .preview-section {
    margin-bottom: 1.5rem;
  }

  .preview-section:last-child {
    margin-bottom: 0;
  }

  .preview-label {
    font-size: 0.7rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: #64748b;
    margin-bottom: 0.75rem;
    font-family: 'JetBrains Mono', monospace;
  }

  .preview-row {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .modal-container {
    background: rgba(0, 0, 0, 0.5);
    border-radius: 8px;
    padding: 2rem;
    display: flex;
    justify-content: center;
  }

  .modal {
    background: #0f172a;
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 8px;
    width: 100%;
    max-width: 400px;
    overflow: hidden;
  }

  .modal-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 1rem 1.25rem;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  }

  .modal-title {
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 1rem;
    font-weight: 600;
    color: #ffffff;
    margin: 0;
  }

  .modal-close {
    background: none;
    border: none;
    color: #64748b;
    font-size: 1.5rem;
    cursor: pointer;
    padding: 0;
    line-height: 1;
  }

  .modal-body {
    padding: 1.25rem;
  }

  .modal-body p {
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 0.875rem;
    color: #94a3b8;
    margin: 0;
    line-height: 1.5;
  }

  .modal-footer {
    display: flex;
    justify-content: flex-end;
    gap: 0.75rem;
    padding: 1rem 1.25rem;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
  }

  .btn {
    font-family: 'Inter', system-ui, sans-serif;
    font-size: 0.875rem;
    font-weight: 500;
    padding: 0.5rem 1rem;
    border-radius: 0;
    cursor: pointer;
    transition: all 0.2s ease;
    border: none;
  }

  .btn-ghost {
    background: transparent;
    color: #94a3b8;
  }

  .btn-primary {
    background: #fbbf24;
    color: #020617;
  }

  .size-badge {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.7rem;
    padding: 0.25rem 0.5rem;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 4px;
    color: #94a3b8;
  }
</style>
